# GitHub Pages deshabilitado — el deploy web pasa a Cloudflare Pages
# (ver deploy-cloudflare.yml y docs/DEPLOY.md).
#
# Para reactivar temporalmente, restaurá el workflow desde git history.

name: Deploy GitHub Pages (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "GitHub Pages está apagado. Usá Deploy Cloudflare Pages."
          exit 1
